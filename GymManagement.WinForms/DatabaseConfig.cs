namespace GymManagement.WinForms
{
    public static class DatabaseConfig
    {
        // Simple connection string for ADO.NET (Must match your PostgreSQL config)
        public static string ConnectionString = "Host=localhost;Port=5432;Database=gym_db;Username=postgres;Password=postgres";
    }
}
