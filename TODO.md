# TODO: Run GymManagementProject

## Approved Plan Steps
- [ ] Step 1: Restore .NET dependencies (`dotnet restore`)
- [ ] Step 2: Install Angular dependencies (`cd gym-management-ui && npm install`)
- [ ] Step 3: Run Backend API (`dotnet run --project GymManagement.API --launch-profile http`) - keep running
- [ ] Step 4: Run Angular UI (`cd gym-management-ui && ng serve`) - keep running  
- [ ] Step 5: Verify (http://localhost:4200 for UI, http://localhost:5220 for API)
- [ ] Step 6: Optional WinForms (`dotnet run --project GymManagement.WinForms`)

**Status**: Starting Step 1
